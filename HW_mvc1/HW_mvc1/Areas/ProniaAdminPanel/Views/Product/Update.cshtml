@model UpdateProductVM

<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Update Product</h4>
                        <form class="forms-sample" method="post" enctype="multipart/form-data">
                            @* <div class="form-group">
                            <label for="Image">Photo</label>
                            <input asp-for="Image" class="form-control" placeholder="Image" accept="image/*">
                            <span class="text-danger" asp-validation-for="Image"></span>
                            </div> *@
                            <div class="form-group">
                                <label for="exampleInputName1">Name</label>
                                <input asp-for="Name" class="form-control" placeholder="Name">
                                <span class="text-danger" asp-validation-for="Name"></span>
                            </div>
                            <div class="form-group">
                                <label for="Price">Price</label>
                                <input asp-for="Price" class="form-control" placeholder="Price">
                                <span class="text-danger" asp-validation-for="Price"></span>
                            </div>
                            <div class="form-group">
                                <label for="SKU">SKU</label>
                                <input asp-for="SKU" class="form-control" placeholder="SKU">
                                <span class="text-danger" asp-validation-for="SKU"></span>
                            </div>
                            <div class="form-group">
                                <label asp-for="CategoryId">Category</label>
                                <select class="form-control" asp-for="CategoryId" asp-items="new SelectList(Model.Categories,nameof(Category.Id), nameof(Category.Name))">
                                    <option disabled selected>Choose Category</option>
                                </select>
                                @* <select class="form-control" asp-for="CategoryId">
                                <option disabled selected>Choose Category</option>
                                @foreach (Category ctg in Model.Categories)
                                {
                                <option value="@ctg.Id">@ctg.Name</option>
                                }
                                </select> *@
                                <span class="text-danger" asp-validation-for="CategoryId"></span>
                            </div>
                            <div class="form-group">
                                <label asp-for="ColorId">Color</label>
                                <select class="form-control" asp-for="ColorId" asp-items="new SelectList(Model.Colors,nameof(Color.Id), nameof(Color.Name))">
                                    <option disabled selected>Choose Color</option>
                                </select>
                                <span class="text-danger" asp-validation-for="ColorId"></span>
                            </div>
                            <div class="form-group">
                                <label asp-for="SizeId">Size</label>
                                <select class="form-control" asp-for="SizeId" asp-items="new SelectList(Model.Sizes,nameof(Size.Id), nameof(Size.Name))">
                                    <option disabled selected>Choose Size</option>
                                </select>
                                <span class="text-danger" asp-validation-for="SizeId"></span>
                            </div>
                            <div class="form-group">
                                <label for="Description">Description</label>
                                <input asp-for="Description" class="form-control" placeholder="Description">
                                <span class="text-danger" asp-validation-for="Description"></span>
                            </div>
                            <button type="submit" class="btn btn-primary me-2">Update</button>
                            <button class="btn btn-light">Cancel</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>